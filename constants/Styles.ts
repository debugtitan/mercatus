import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  appLayout: {
    flex: 1,
    height: '100%',
    position: 'relative',
  },
  layoutContainer: {
    marginHorizontal: 4,
  },
  centerImage: {
    display: 'flex',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
